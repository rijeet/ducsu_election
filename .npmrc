# Allow optional dependencies but target Linux platform for Vercel
optional=true
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
# Target Linux platform for Vercel deployment
target_platform=linux
target_arch=x64
